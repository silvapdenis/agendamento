{
  "name": "medical-appointment-bot",
  "version": "1.0.0",
  "description": "Medical appointment system with WhatsApp integration via n8n",
  "main": "public/index.php",
  "scripts": {
    "build": "composer install --optimize-autoloader --no-dev && php artisan key:generate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache",
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT",
    "deploy": "railway deploy",
    "migrate": "php artisan migrate --force",
    "seed": "php artisan db:seed --force"
  },
  "engines": {
    "php": "^8.1",
    "node": "18.x"
  },
  "dependencies": {
    "railway": "^3.0.0"
  },
  "railway": {
    "build": {
      "buildCommand": "composer install --optimize-autoloader --no-dev"
    },
    "deploy": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
      "healthcheckPath": "/api/health"
    }
  }
}